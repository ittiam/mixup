{
  "name": "my-mixup-app",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "start": "mixup start",
    "build": "mixup build",
    "test": "mixup test --env=jsdom"
  },
  "devDependencies": {
    "@mixup/mixup": "^2.0.5",
    "eslint-config-mixup": "^1.0.0",
    "less": "^3.10.3",
    "less-loader": "^5.0.0",
    "mixup-plugin-vue": "^2.0.5",
    "stylelint-config-mixup": "^1.0.0"
  },
  "dependencies": {
    "vue": "^2.6.10"
  },
  "browserslist": [
    "ff >= 26",
    "iOS >= 6",
    "Android >= 4",
    "ChromeAndroid >= 40",
    "ie >= 9"
  ]
}
